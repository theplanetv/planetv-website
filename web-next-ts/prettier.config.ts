import { Config } from "prettier";

const config: Config = {
  tabWidth: 2,
  semi: false,
  singleQuote: false,
  trailingComma: "es5",
  bracketSpacing: true,
  arrowParens: "always",
  endOfLine: "lf",
  printWidth: 80,
};

export default config;
