stages:
  - build

build-docker:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker-compose up -d

enter-nix-dev-shell:
  stage: build
  image: nixpkgs/nix-flakes:latest
  services:
    - docker:dind
  script:
    - nix develop .
